// background.js
chrome.runtime.onMessage.addListener(async (message, sender, sendResponse) => {
  switch (message.type) {
    // Add cases for message types from copilot-instructions.md
    default:
      // No-op for now
      break;
  }
});
