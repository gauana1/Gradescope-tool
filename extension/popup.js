// popup.js
window.addEventListener('DOMContentLoaded', async () => {
  // Read courses from chrome.storage.local
  // Render course list
  // No-op for now
});

document.getElementById('upload-btn')?.addEventListener('click', () => {
  // Trigger upload for selected courses
  // No-op for now
});
